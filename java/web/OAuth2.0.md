OAuth 2.0 标准是在 [RFC 6749](https://tools.ietf.org/html/rfc6749) 规范定义的。
OAuth 的核心就是向第三方应用颁发令牌。

OAuth 2.0 协议特征：

- 授权协议
   授权是 Oauth 2.0 思想的核心，其目的就在于拥有了授权，就能够通过授权取得权限范围内的受保护资源
- 委托协议
   当用户授权第三方软件获取部分用户数据时，其本质就是资源所有者委托了第三方进行资源的使用
- 安全协议
   安全问题永远是互联网最为核心的问题，OAuth 2.0 协议本身就推荐了相当多的安全处理方案，因此说 OAuth 2.0 也是安全协议

参考微服务 OAuth 2.0 [网址](https://www.kaper.com/?s=MICRO-SERVICES+ARCHITECTURE+WITH+OAUTH2+AND+JWT)

认证(Authentication) 和授权（Authorization）总是成对出现，但是不能混为一谈。